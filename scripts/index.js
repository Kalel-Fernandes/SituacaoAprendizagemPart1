function addTask() {
    const name = document.getElementById('name-task');
    const time = document.getElementById('hours');
    const costHour = document.getElementById('hourly-rate');

    const validateInput = () => inputElement.value.trim().length > 0;
}